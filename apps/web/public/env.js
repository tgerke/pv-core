// Dev stub. In production nginx overrides /env.js with values from the
// container environment (see nginx/default.conf.template); Vite copies this
// file into dist so the path always resolves.
window.__PV_ENV__ = {};

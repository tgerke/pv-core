# Requirement traceability matrix

Generated from a live test run (commit 2e345a8, node v23.11.0, 2026-08-17T16:12:39.226Z); regenerate with `pnpm validation:artifacts`.
Join key: the regulatory token appearing verbatim in test names, so this matrix cannot
drift from the suite without showing it.

| Requirement | Mechanism | Verifying tests | Result |
| --- | --- | --- | --- |
| §11.10(a) validation of systems | **Not claimed.** The raw material is generated, not hand-written: `pnpm validation:iq` checks a live environment's controls and `pnpm validation:artifacts` emits an OQ run report and a requirement→test traceability matrix from a live suite run (ADR-0017). Formal CSV remains an organizational program | *(no automated test)* | none |
| §11.10(b) accurate and complete copies | Attachments and submitted payloads are content-addressed (SHA-256) and immutable; the API serves the exact bytes at `/files/{sha256}` scoped to the case. Every version hashes to `pv_case_version_sha256()`; `GET /case-versions/{id}/e2b.json` exports the version keyed by E2B(R3) element IDs, and `/cioms1.pdf` and `/medwatch-3500a.pdf` render the CIOMS I and Form FDA 3500A layouts transcribed from the official documents, each page naming the version hash (ADR-0012). Recording a submission stores the rendered bytes it sent | local blob store (ADR-0013, §11.10(b)) stores by sha256 and reads back identical bytes<br>local blob store (ADR-0013, §11.10(b)) deduplicates identical content and misses unknown hashes<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) walks a case from intake to acknowledged submission and a follow-up<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) serves the DSUR views, compliance metrics, and rule matches<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) validates bodies and reports zod issues as 400 | PASS |
| §11.10(c) record protection & retention | Signatures, submissions, acknowledgements, attachments, transitions, unblinding, nullification, listed RSI terms, dictionaries, and audit events reject UPDATE/DELETE via database triggers, for every role. A case version and its children reject them once a signature exists. Cases are never deleted | append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on audit_event at the database level<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on signature (§11.70 binding stays intact)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on submission and submission_acknowledgement<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on the case facts: transitions, unblinding, nullification, attachments<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) never deletes a case; identity columns cannot change (E2B(R3) C.1.8.1)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) locks a signed version and its children (ADR-0006)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects further versions of a nullified case (C.1.11)<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot TRUNCATE domain tables<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot disable triggers (not the table owner)<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot run DDL in the schema<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot write audit_event directly, yet its DML is still audited<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) holds no UPDATE/DELETE privilege on immutable facts even before the trigger fires | PASS |
| §11.10(d) limited system access | `AUTH_MODE=oidc`: JWTs validated against the IdP's issuer, audience, and JWKS; the verified email claim resolves to a person, or the request is refused; there is never a fallback actor. The API runs as a least-privilege DB role (`pv_app`: DML only, no TRUNCATE/DDL, no direct audit writes). A dev-token mode remains for the demo and is not a Part 11 posture | least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot TRUNCATE domain tables<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot disable triggers (not the table owner)<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot run DDL in the schema<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) cannot write audit_event directly, yet its DML is still audited<br>least-privilege runtime role pv_app (§11.10(c) §11.10(d)) holds no UPDATE/DELETE privilege on immutable facts even before the trigger fires<br>dev-mode authentication (§11.10(d)) refuses requests without a bearer token (401) and unknown tokens<br>dev-mode authentication (§11.10(d)) resolves a token to a person and their grants<br>OIDC authentication (§11.10(d), ADR-0016) accepts a valid token and resolves the person by verified email claim<br>OIDC authentication (§11.10(d), ADR-0016) rejects a token for the wrong audience and a forged token (wrong key)<br>OIDC authentication (§11.10(d), ADR-0016) rejects an authenticated identity with no person record (403, never a fallback actor)<br>OIDC authentication (§11.10(d), ADR-0016) rejects a token whose email is explicitly unverified<br>OIDC authentication (§11.10(d), ADR-0016) maps a machine identity by subject (API_SERVICE_SUBJECTS) to the enter-only intake person | PASS |
| §11.10(e) audit trails | Computer-generated, timestamped `audit_event` rows written by AFTER-triggers on every domain-table mutation; append-only; prior values preserved as full row images; **hash-chained** so retroactive edits are detectable (`pv_verify_audit_chain()`, `GET /audit-chain/verify`) | append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on audit_event at the database level<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on signature (§11.70 binding stays intact)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on submission and submission_acknowledgement<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on the case facts: transitions, unblinding, nullification, attachments<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) never deletes a case; identity columns cannot change (E2B(R3) C.1.8.1)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) locks a signed version and its children (ADR-0006)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects further versions of a nullified case (C.1.11)<br>audit trail (§11.10(e), ADR-0003) writes an attributed, chained event for every domain mutation<br>audit trail (§11.10(e), ADR-0003) verifies clean on untampered data<br>audit trail (§11.10(e), ADR-0003) detects tampering when a row is altered with triggers disabled | PASS |
| §11.10(g) authority checks | Role-based grants (`access_grant`: admin / case_processor / medical_reviewer / read_only / ingest → read / enter / assess / sign / submit / administer, scoped to a sponsor organization, a study, or unscoped) enforced per route; grant changes are themselves audited and revocation is a fact. Actor identity is bound per transaction (`pv.actor_id`) | authorization over grants (§11.10(g), ADR-0015, ADR-0016) a sponsor-scoped reviewer assesses that sponsor's studies and no others<br>authorization over grants (§11.10(g), ADR-0015, ADR-0016) a study-scoped processor enters and submits for that study only<br>authorization over grants (§11.10(g), ADR-0015, ADR-0016) the ingest identity enters and never reads, assesses, signs, or submits<br>authorization over grants (§11.10(g), ADR-0015, ADR-0016) an unscoped admin does everything; read_only reads and nothing else<br>authorization over grants (§11.10(g), ADR-0015, ADR-0016) readableScope narrows list queries to the grants' sponsors and studies<br>authorization by grant scope (§11.10(g), ADR-0015) a sponsor-scoped reviewer never sees the other sponsor's cases; the unscoped admin sees all<br>authorization by grant scope (§11.10(g), ADR-0015) the ingest identity can create a case but read nothing (403)<br>authorization by grant scope (§11.10(g), ADR-0015) read_only cannot write; processors cannot assess or sign; reviewers cannot submit | PASS |
| §11.50 signature manifestation | `signature` rows carry signer, timestamp, and meaning (medical_review / approval); the case page displays all three | e-signatures (§11.50 §11.70 §11.200) a signature copies the version hash and records how the signer re-authenticated<br>e-signatures (§11.50 §11.70 §11.200) the database refuses a signature without re-authentication evidence<br>e-signatures (§11.50 §11.70 §11.200) a submission needs an approval signature bound to the current hash and copies that hash; the acknowledgement discharges it (E2B(R3) IG §4.0) | PASS |
| §11.70 signature/record linking | A signature stores the version's SHA-256 at signing; the first signature locks the version so the binding cannot drift; `v_signature_integrity` recomputes the hash on demand and `submission` rows copy the hash they sent, which the database checks against an approval signature | case versions lock on signature (ADR-0006, §11.70) edits succeed while unsigned and are refused with 'locked' after the first signature<br>case versions lock on signature (ADR-0006, §11.70) a follow-up cannot open while the latest version is unsigned; once signed it clones the children<br>case versions lock on signature (ADR-0006, §11.70) nullification is a fact that blocks further versions and signatures<br>case versions lock on signature (ADR-0006, §11.70) the sender case id is CC-org-year-number and doubles as the worldwide id (C.1.1, C.1.8.1)<br>e-signatures (§11.50 §11.70 §11.200) a signature copies the version hash and records how the signer re-authenticated<br>e-signatures (§11.50 §11.70 §11.200) the database refuses a signature without re-authentication evidence<br>e-signatures (§11.50 §11.70 §11.200) a submission needs an approval signature bound to the current hash and copies that hash; the acknowledgement discharges it (E2B(R3) IG §4.0)<br>append-only enforcement (Part 11 §11.10(c) §11.10(e)) rejects UPDATE and DELETE on signature (§11.70 binding stays intact)<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) walks a case from intake to acknowledged submission and a follow-up<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) serves the DSUR views, compliance metrics, and rule matches<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) validates bodies and reports zod issues as 400 | PASS |
| §11.200 signature components | Signing requires re-authentication: in OIDC mode a freshly issued token for the same subject with `auth_time` inside a short window (default 300 s); method and time are recorded on the signature row and are NOT NULL from birth. The dev-mode stub restates the bearer token: API-shape parity, not a credential challenge | e-signatures (§11.50 §11.70 §11.200) a signature copies the version hash and records how the signer re-authenticated<br>e-signatures (§11.50 §11.70 §11.200) the database refuses a signature without re-authentication evidence<br>e-signatures (§11.50 §11.70 §11.200) a submission needs an approval signature bound to the current hash and copies that hash; the acknowledgement discharges it (E2B(R3) IG §4.0)<br>signing re-authentication (§11.200) rejects a signing request whose re-authentication does not match the session<br>OIDC signing re-authentication (§11.200) accepts a fresh re-auth token for the same subject and records oidc_fresh_token<br>OIDC signing re-authentication (§11.200) rejects a stale re-auth token (auth_time outside the freshness window)<br>OIDC signing re-authentication (§11.200) rejects a re-auth token minted for a different subject | PASS |
| Seriousness criteria (E2A §II.B; E2B(R3) E.i.3.2 a–f) | Six booleans per event; a fatal outcome (E.i.7 = 5) requires the death criterion (CHECK) | guards (0001): CHECK constraints the schema carries a fatal outcome requires the death seriousness criterion (E2A §II.B; E2B(R3) E.i.3.2a, E.i.7) | PASS |
| Expectedness against the reference safety information (E2A §II.C; the RSI version at the moment of occurrence, Reg. 536/2014 Annex III §2.2(8)) | `product_rsi_version` + `rsi_listed_term`; `v_case_event_reportability` derives expectedness against the version in effect at event onset, records the basis, and applies a sponsor override only when a rationale is recorded (E2A §II.C.2) | expectedness against the RSI in effect at onset (E2A §II.C; Reg. 536/2014 Annex III §2.2(8)) a term listed only in the later RSI is unexpected for an onset before that version took effect<br>expectedness against the RSI in effect at onset (E2A §II.C; Reg. 536/2014 Annex III §2.2(8)) and expected once the listing RSI is in effect at onset<br>expectedness against the RSI in effect at onset (E2A §II.C; Reg. 536/2014 Annex III §2.2(8)) a sponsor override with a rationale wins (E2A §II.C.2)<br>expectedness against the RSI in effect at onset (E2A §II.C; Reg. 536/2014 Annex III §2.2(8)) no RSI in effect: unexpected, fail-safe, with the basis saying so<br>guards (0001): CHECK constraints the schema carries requires an expectedness override and its rationale together (E2A §II.C.2) | PASS |
| What to expedite: serious, unexpected, and reasonably related, judged event by event (E2A §III.A.1) | `v_case_reportability` and `v_rule_match`; unassessed causality counts as related and is flagged, so the fail-safe direction is to over-report (ADR-0007) | expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) serious but expected (listed in the RSI in effect): no expedited obligation (E2A §III.A.1) | PASS |
| Clocks: 7 calendar days for fatal/life-threatening unexpected reactions, 15 for other serious unexpected (E2A §III.B.1–2; 21 CFR 312.32(c)(2), (c)(1); Reg. 536/2014 Art. 42(2)(a)–(b)); follow-up information within 15 days (312.32(d); Annex III §2.4) | Rules are rows (`reporting_rule`); `pv_sync_expected_submissions()` materializes `expected_submission` rows from the awareness date; `v_expected_submission_status` derives due-soon / overdue / submitted / acknowledged / superseded / not-required on every read | expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) fatal, unexpected, related: a 7-day and a 15-day obligation from the awareness date<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) serious, unexpected, related but not fatal: 15-day only<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) serious but expected (listed in the RSI in effect): no expedited obligation (E2A §III.A.1)<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) non-serious: nothing<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) not related by both reporter and sponsor: nothing<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) unassessed causality is treated as related (fail-safe, ADR-0007) and flagged<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) re-syncing is idempotent and a change that removes reportability removes the undischarged obligation<br>follow-ups and discharge (21 CFR 312.32(d); Reg. 536/2014 Annex III §2.4) a submitted initial obligation is discharged; a follow-up version opens a new 15-day obligation from its own day zero<br>follow-ups and discharge (21 CFR 312.32(d); Reg. 536/2014 Annex III §2.4) an initial obligation the latest version no longer triggers reads superseded_by_follow_up<br>follow-ups and discharge (21 CFR 312.32(d); Reg. 536/2014 Annex III §2.4) a waiver reads not_required (E2A §III.E.1 placebo, protocol-defined endpoints)<br>guards (0001): CHECK constraints the schema carries requires a rationale when the awareness date differs from the receipt date (E2A §III.B.3, ADR-0007)<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) walks a case from intake to acknowledged submission and a follow-up<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) serves the DSUR views, compliance metrics, and rule matches<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) validates bodies and reports zod issues as 400 | PASS |
| Day zero is first knowledge that the minimum criteria are met (E2A §III.B.3) | `case_version.awareness_date`, defaulted to the receipt date, with a required rationale when they differ; no obligation exists before the version meets the minimum criteria | expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) fatal, unexpected, related: a 7-day and a 15-day obligation from the awareness date<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) serious, unexpected, related but not fatal: 15-day only<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) serious but expected (listed in the RSI in effect): no expedited obligation (E2A §III.A.1)<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) non-serious: nothing<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) not related by both reporter and sponsor: nothing<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) unassessed causality is treated as related (fail-safe, ADR-0007) and flagged<br>expedited clocks (ICH E2A §III.B; 21 CFR 312.32(c)(1)-(2)) re-syncing is idempotent and a change that removes reportability removes the undischarged obligation<br>guards (0001): CHECK constraints the schema carries requires a rationale when the awareness date differs from the receipt date (E2A §III.B.3, ADR-0007)<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) walks a case from intake to acknowledged submission and a follow-up<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) serves the DSUR views, compliance metrics, and rule matches<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) validates bodies and reports zod issues as 400 | PASS |
| Minimum valid ICSR: identifiable patient, identifiable reporter, one event, one suspect drug (E2B(R3) IG §3.3.1) | `v_case_minimum_criteria`; medical review, signing, and submission are refused before it holds | minimum criteria for a valid ICSR (E2B(R3) §3.3.1) an intake item without a reporter is not valid, has no clock, and shows as intake<br>minimum criteria for a valid ICSR (E2B(R3) §3.3.1) no medical review, signature, or submission before validity<br>minimum criteria for a valid ICSR (E2B(R3) §3.3.1) adding the reporter makes it valid and materializes the clock<br>minimum criteria for a valid ICSR (E2B(R3) §3.3.1) a case with only concomitant drugs is not valid (needs a suspect or interacting drug)<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) walks a case from intake to acknowledged submission and a follow-up<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) serves the DSUR views, compliance metrics, and rule matches<br>case lifecycle over HTTP (E2B(R3) §3.3.1; E2A §III.B; §11.70; §11.10(b)) validates bodies and reports zod issues as 400 | PASS |
| Single-subject unblinding for expedited cases (E2A §III.D; Annex III §2.5) | `case_unblinding` fact recorded from the randomization system's code-break; arms never leave that table except through the DSUR aggregate (ADR-0008) | DSUR line listing (ICH E2F §3.7.2) prints the arm only where an unblinding fact exists and 'blinded' otherwise (E2A §III.D)<br>read-only role pv_readonly (ADR-0008, E2A §III.D: arms at rest stay out of reach) reads the derived views<br>read-only role pv_readonly (ADR-0008, E2A §III.D: arms at rest stay out of reach) cannot read the arm columns of case_unblinding<br>read-only role pv_readonly (ADR-0008, E2A §III.D: arms at rest stay out of reach) cannot write anything | PASS |
| DSUR line listing of serious adverse reactions and cumulative SAE tabulation by SOC and arm (E2F §3.7.2, §3.7.3) | `v_dsur_sar_line_listing`, `v_dsur_sae_summary`, served at `/dsur/*` | DSUR line listing (ICH E2F §3.7.2) lists one row per case under its most serious reaction, ordered by trial, SOC, PT<br>DSUR line listing (ICH E2F §3.7.2) prints the arm only where an unblinding fact exists and 'blinded' otherwise (E2A §III.D)<br>DSUR line listing (ICH E2F §3.7.2) excludes nullified cases<br>DSUR cumulative SAE tabulation (ICH E2F §3.7.3) counts serious events, cases, and reactions by SOC and arm | PASS |
| Regulator acknowledgements (E2B(R3) IG §4.0) | `submission_acknowledgement` with the AA/AE/AR/CA/CR codes or a manual receipt; a rejected acknowledgement leaves the obligation visibly `submitted` | e-signatures (§11.50 §11.70 §11.200) a submission needs an approval signature bound to the current hash and copies that hash; the acknowledgement discharges it (E2B(R3) IG §4.0) | PASS |

Requirements without automated verification: §11.10(a). See the mechanism column and docs/03-compliance.md for their status (some are documented as not claimed).

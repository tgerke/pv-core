# Test fixtures

`meddra-mini/` is a five-term fixture in the `$`-delimited layout the importer
expects (see `src/meddra.ts`). The codes are synthetic and the hierarchy is
illustrative; it exists so `import-meddra.test.ts` can exercise the loader
without a licensed distribution. It is not MedDRA.
